<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.devpicon.android.myconstraintlayoutlabapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DimensionRatioActivity"></activity>
        <activity android:name=".SizeableConstraintActivity" />
        <activity
            android:name=".Demo1Activity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BarrierActivity" />
        <activity android:name=".GuidelineActivity" />
        <activity android:name=".SteroidRelativeLayout2Activity" />
        <activity android:name=".SteroidRelativeLayoutActivity" />
        <activity
            android:name=".LikeLinearLayoutActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>